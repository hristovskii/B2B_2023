/*
Од стандарден влез се вчитуваaт три броја кои ги означуваат времињата 
(во минути) со кои три натпреварувачи истрчале 5км на Скопскиот маратон. 

Да се испечати редниот број на најбрзиот натпреварувач. 
Доколку најбрзо време имаат повеќе од еден натпреварувач, 
се печатат редните броеви на сите такви натпреварувачи.*/
#include<stdio.h>
int main () {
    float a,b,c;
    scanf("%f %f %f", &a, &b, &c);
    if (a<=b && a<=c){
        printf("1");
    }
    if (b<=a && b<=c){
        printf("2");
    }
    if (c<=a && c<=b) {
        printf("3");
    }
    return 0;
}